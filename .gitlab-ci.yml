sequential:
    script:
        - kill $(pgrep -f flask) || echo "no flask server running"
        - rm -rf /srv/backend/
        - mkdir /srv/backend/
        - cp -R * /srv/backend/
        - bash /srv/backend/production_start.sh
        - echo 12311231 | sudo -S sudo systemctl restart nginx
        - echo 12311231 | sudo systemctl start SurveyBackend